# SmartTrading-D366

Flutter mobile app + simple backend (Flask) — starter for a trading assistant.

Quick start:
1. Install Flutter (stable channel) on your machine.
2. Clone repo and run:
   flutter pub get
   flutter run  # for debug
   flutter build apk --release  # for release APK

Backend:
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
