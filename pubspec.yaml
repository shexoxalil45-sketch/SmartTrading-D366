name: smarttrading_d366
description: SmartTrading-D366 — AI Trading Assistant (mobile + backend)
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # network
  http: ^1.2.2
  web_socket_channel: ^2.4.0

  # ui & webview
  webview_flutter: ^4.0.7
  provider: ^6.1.2
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # local storage & secure
  shared_preferences: ^2.0.15
  flutter_secure_storage: ^9.2.2

  # charts/visuals
  fl_chart: ^0.69.0
  lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/lang/
    - assets/icons/
